{
    "name": "[% config.base %]",
    "abstract": "[% config.abstract %]",
    "version": "[% config.version %]",
    "author": [
        "[% config.author %] <[% config.email %]>"
    ],
    "license": [ "perl" ],
    "no_index": {
        "directory": ["t", "inc", "tmpl", "build"]
    },
    "resources": {
        "MailingList": "[% config.MailingList %]",
        "bugtracker": {
            "web" : "[% config.rt %]"
        }
        "repository": {
            "url": "[% config.repo %]"
        }
    }
}
